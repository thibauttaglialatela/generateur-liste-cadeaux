# config/packages/ai.yaml
ai:
    platform:
        mistral:
            api_key: '%env(MISTRAL_API_KEY)%'

    agent:
        gift_generator:
            platform: mistral
            model: mistral-small-latest
            options:
                temperature: 0.7
                max_tokens: 300